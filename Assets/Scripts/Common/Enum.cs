public enum PlayerState
{
    Idle,
    Moving,
    Attacking,
    Dead
}

public enum MonsterState
{
    Idle,
    Moving,
    Attacking,
    Dead
}
public enum ItemType
{
    Weapon,
    Armor,
    Ring,
    Rune
}